'use strict';

var blog = require('../app/app.js'),
should = require('should'),
request = require('supertest'),
User = require('../app/models/user');

describe('Users', function () {

  before(function (done) {
    done();
  });

  it.skip('should have tests', function (done) {
    done();
  });

  after(function (done) {
    done();
  });

});